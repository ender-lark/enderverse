# Research Backlog Burn — REPORT 2026-06-13

Agent: **Claude Code**, autonomous overnight run.
Branch: **`feat/research-backlog-burn`**. Evidence as-of **2026-06-12** close
(last market-open date; today is Sat 2026-06-13). Tests: **1442 passed, 6 skipped,
0 failures** (full `pytest src/`). No buy/sell actions written; no ACT_NOW invented.

## Rows read
- **Seed list:** 8 operator-provided backlog items (all carried through the gate).
- **Notion 📚 Research Queue** (`cab89576-0933-40b0-ad2e-6f9a6188e804`): ~25 rows
  surfaced via semantic search; **4 fetched in full** (GOOGL, BMNR, WQTM, and the
  Power & Policy lane row). *Caveat: the Notion MCP exposes search + per-page
  fetch, not a full ordered table dump with every `Status`* — so triage was
  anchored on the seed list + fully-fetched rows; un-verifiable rows were marked
  UNCERTAIN, not killed.
- **Repo feed** (`src/research_queue.json`): 3 pending rows (AVGO, BMNR, GOOGL).
- `docs/queue_audit_2026-06-12.md` (T6) **does not exist yet** → nothing
  pre-excluded.

## Proposed kills (2) — operator approves before any Notion change
1. **Pre-event trigger registration discipline** (`372c5031…abc8`) — superseded:
   the trigger spine is shipped (T1 `trigger_registry.json`, PR#15 MERGED, arming
   EWRE / GOOGL / ASTS / RKLB). Lesson implemented; nothing forward-looking. Gate (c).
2. **Reddit Signal Pipeline — system upgrade** (`35fc5031…8130`) — superseded by
   the 2026-06-13 "Reddit → Power & Policy political-pulse" reframe, and out of
   main-build scope per `AGENTS.md`. Gate (c) + scope; refile under the reddit
   handoff.

**Conflict surfaced (NOT killed): MAGS.** The "fully exited in the 6/12 rotation"
premise is **refuted by repo truth** — `positions.json` (2026-06-12) still lists
MAGS 253 sh = **$16,422** and `theses.json` has `thesis_mags` **ACTIVE**. No MAGS
kill proposed; if MAGS was truly exited, the positions file is stale and needs
reconciliation first.

## Relevant rows burned (9 full dossiers) + 5 UNCERTAIN mini-dossiers
Full dossiers (evidence-only, as-of 2026-06-12): GOOGL · BMNR · TSM · HOOD · AVGO ·
CCJ · USAR · AI-energy/power-bottleneck · WQTM. UNCERTAIN (keep/kill questions in
TRIAGE): CXW · FN · FIGR · INTC · Cybersecurity dig.

## The 5 most actionable dossiers
1. **GOOGL — WATCH.** Armed `googl-tranche-2-review-2026-06-19` trigger + 6/14
   operator review; $84.75B financing absorbed inside the $356–366 range with
   mild-positive flow. Resolve dilution math + NSPM-11 before the add confirms.
2. **WQTM — soft NO-GO (close the loop).** The go/no-go has rotted 17 days; entry
   decayed ~5% off the 5/26 spike, IONQ -8% wk, still no named FS/Meridian/Lee
   anchor. Recommend GO-small on explicit approval *or* shelve to MONITOR.
3. **BMNR — MONITOR.** Quantify mNAV / ETH-per-share / 9.50%-preferred coverage
   before any defend/add; IV rank 7 says no near catalyst is priced. Gated.
4. **HOOD — RESEARCH (flag only).** First lane-vs-FS conflict; +9.6% on the week
   into range highs with frothy two-sided flow. Needs operator adjudication, not
   an average.
5. **AVGO — RESEARCH (operational).** Reconcile the **~$40K untracked holding**
   (absent from `positions.json`) and write the standalone AI-networking/ASIC
   thesis; the print catalyst has already passed.

## Biggest evidence gaps
- **T4 deepdive battery NOT run** (multi-day OI build, 10-day dark-pool blocks,
  greek exposure) — T4-owned; called out as MISSING in every dossier rather than
  rebuilt.
- **Fundamentals/valuations not pulled** for any name (price + options-flow only).
- **BMNR mNAV / ETH-per-share / dividend-coverage** and **GOOGL dilution math** —
  the two decision-critical computations — unresolved.
- **Power & Policy lane packets** (Geo Risk Register, Policy Money Map) are **held
  to the operator's 6/14 review** — referenced, not duplicated; they own the
  policy framing for TSM (situation #2), AI-energy (situation #6), USAR/CCJ
  (Policy Money Map new names), and the HOOD/GOOGL flags.
- **WQTM named-anchor check** (FS deck) + **$2B LOI-close status** not verified.
- **Account-level sizes** for GOOGL/AVGO — `positions.json` is read-only and does
  not list them.

## Ghost-routine audit — VERDICT: both are REAL, not ghosts
- **Off-Hours Research Queue** (`investing-os-off-hours-research-queue`, weekdays
  7:30 PM ET): fired **2026-06-12 19:31 ET** with a `started`+`success`
  scheduled receipt pair, summary *"research queue drop missing; not checked;
  cache unchanged"* — i.e. it ran and correctly honored the honesty rail on a
  missing input.
- **Top Prospects Auto-Research** (`investing-os-top-prospects-auto-research`,
  daily 8:45 PM ET): fired **2026-06-12 20:46 ET** with a `started`+`success`
  pair, summary *"dry-run BUILDING 8; HOT 0 so queued none; remaining unqueued 21;
  missing pulls remain."*
- **Already wired into the cadence-aware overdue stack:** both are present in
  `cloud_ops_status.DEFAULT_EXPECTED_AUTOMATIONS` with full cadence
  (days/hour/minute/`expected_since`), so a silent death already pages the
  dashboard via the overdue-gap path.
- **Important correction to the task premise:** "zero receipts for either" was
  true **relative to `main`**, but **false on this branch** — the
  `fix/rail-clipboard-fallback` history (which this branch is based on) already
  added the first scheduled receipts *and* the overdue wiring. My contribution is
  therefore a **regression guard** (`test_research_routines_overdue_guard.py`)
  that locks the wiring in and proves a missing scheduled receipt surfaces as
  overdue, plus documenting the receipt-every-run + triage-gate routine in
  `off_hours_research.md`. I did **not** rebuild the (already-working) emit path.

## Dated events discovered → routed to operator / C5 (not edited here)
- **FOMC 2026-06-16/17** — flagged missing from `catalysts.json`; C5 owns it.
- **Sunday 2026-06-14 night** operator review (rebalance + Geo Risk Register +
  Policy Money Map all HELD until GO).
- **GOOGL 2026-06-19** tranche-2 trigger (armed, expires 6/23).

## Blockers hit and skipped (continued past)
1. **`main` is checked out in another git worktree** → could not branch off main;
   branched off current HEAD (`fix/rail-clipboard-fallback`) instead. Merging main
   in is forbidden by guardrail, so the PR diff vs main will include pre-existing
   `fix/rail-*` commits. Full test suite is green regardless.
2. **Notion enumeration limited** to semantic search + per-page fetch (no full
   table dump) → conservative triage; only repo-truth-defensible kills proposed.
3. **Notion write-back limited to 3 rows** (GOOGL, BMNR, WQTM) — the only
   processed rows with confirmed Notion page IDs. TSM/CCJ/USAR/HOOD/AI-energy are
   seed/lane-derived and have no distinct Notion row to comment on. The 3 comments
   posted successfully (additive only; no Status/field changes).
4. **`catalysts.json` read-only + C5-owned** → dated events reported, not written.
5. **T4 / T6 ownership respected** → deepdive battery not rebuilt; no Notion
   queue-audit cleanup re-run.

## What shipped (commits on `feat/research-backlog-burn`)
- Workboard claim (R1–R4).
- **R2:** `--from-notion` intake mode + 3-input-path tests; ghost-routine overdue
  guard; routine-doc update (Notion-first + triage gate + R4 nightly routine).
- **R1:** `TRIAGE_2026-06-13.md`.
- **R3:** 9 dossiers + `INDEX.md` + `src/research_queue.json` research block.
- **R4:** nightly delta-triage routine documented in `off_hours_research.md`.
